# sql

This directory will contain SQL Server schema definitions, migration scripts, indexes, and seed data.

## Planned Contents

- `schema/` — Table definitions (e.g., `Events`, `Aggregates`, `Entities`)
- `indexes/` — Index strategy scripts with commentary on design tradeoffs
- `seed/` — Sample data for local development and testing
- `migrations/` — Versioned migration scripts
